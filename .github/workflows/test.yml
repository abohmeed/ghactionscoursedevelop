on:
  workflow_dispatch:
jobs:
  deploy_summary:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Application
        run: echo "Deployed to https://myapp.example.com"
        
      - name: Add Deployment Link
        run: echo "[🔗 View Deployment](https://myapp.example.com)" >> $GITHUB_STEP_SUMMARY
